#include <iostream>
#include <math.h>
using namespace std;

int main()
{
  float a,b,c,D;
  double x1,x2;
  cout<<"Ingresar el valor a:";
  cin>>a;
  if (a==0)
     cout<<"ERROR: No se puede calcular";
  else {
  cout<<"Ingresar elvalor b:"<<endl;
  cin>>b;
  cout<<"Ingresar el valor c:"<<endl;
  cin>>c;
     D=b*b-4*a*c;
     if (D==0)
     cout<<"La solucion es unica"<<-b/2*a;
  else
     if (D>0){
         x1=(-b-sqrt(b*b-4*a*c)/2*a);
         x2=(-b+sqrt(b*b-4*a*c)/2*a);
         cout<<"El valor de la primera raiz x1 es:"<<x1;
         cout<<"El valor de la segunda raiz x2 es:"<<x2;}
  else {
      x1=-b/(2*a);
      x2=sqrt(-D)/(2*a);
      cout<<"El valor de la primera raiz x1 es:"<<x1;
      cout<<"El valor de la segunda raiz x2 es:"<<x2;}
  }
   return 0;
     }
